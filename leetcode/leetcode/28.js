/**
 * @param {string} haystack
 * @param {string} needle
 * @return {number}
 */
var strStr = function(haystack, needle) {
   let a= haystack.indexOf(needle);
   console.log(a);
   
};

strStr("aaaaa","ba")